language: java

jdk: openjdk-17

services:
  - postgresql

env:
  global:
    # A different port must be used when not using the
    # default PostgreSQL:
    - POSTGRES_USER=user
    - POSTGRES_PASSWORD=pass
    - POSTGRES_DB=articles
    - DB_URL=jdbc:postgresql://localhost:5432/articles

script: cd my-site-back && mvn test
      

